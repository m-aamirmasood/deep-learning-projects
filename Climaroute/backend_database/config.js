/*this file contains the mysql database configuration and can be changed for different
  servers and databases*/
const config = {
  host    : 'localhost',
  user    : 'root',
  password: 'ikidikid',
  database: 'CLIMAROUTE'
};

module.exports = config;  //export the module
